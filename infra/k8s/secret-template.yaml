# TEMPLATE — base64-encode all values before applying.
# kubectl create secret generic dlp-secrets -n mycyber-dlp \
#   --from-literal=JWT_SECRET=<strong-random> \
#   --from-literal=POSTGRES_PASSWORD=<strong-password> \
#   --from-literal=MINIO_ROOT_PASSWORD=<strong-password> \
#   --dry-run=client -o yaml > infra/k8s/secret.yaml
apiVersion: v1
kind: Secret
metadata:
  name: dlp-secrets
  namespace: mycyber-dlp
type: Opaque
data:
  JWT_SECRET: ""          # base64(your-jwt-secret)
  POSTGRES_PASSWORD: ""   # base64(your-pg-password)
  MINIO_ROOT_PASSWORD: "" # base64(your-minio-password)
